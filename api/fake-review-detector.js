import checkRateLimit from '../utils/rateLimit.js';

export default async function handler(req, res) {
  res.setHeader("Access-Control-Allow-Origin", "*");
  res.setHeader("Access-Control-Allow-Methods", "POST, OPTIONS");
  res.setHeader("Access-Control-Allow-Headers", "Content-Type");

  if (req.method === "OPTIONS") return res.status(200).end();
  if (req.method !== "POST") return res.status(405).json({ error: "Method Not Allowed" });

  const rateLimit = checkRateLimit(req, 10, 60000);
  if (!rateLimit.success) {
    return res.status(429).json({ error: "Too many requests. Please try again in a minute." });
  }

  const { review } = req.body;
  if (!review || typeof review !== "string" || review.trim().length === 0)
    return res.status(400).json({ error: "Review text is required." });

  if (review.trim().length > 3000)
    return res.status(400).json({ error: "Review too long. Keep it under 3,000 characters." });

  const groqKey = process.env.GROQ_API_KEY;
  if (!groqKey || groqKey.trim() === "")
    return res.status(500).json({ error: "Server configuration error." });

  const sanitized = review.trim().replace(/[<>]/g, "");

  const systemMessage = `You are an expert AI detection algorithm specialized in analyzing product reviews (Amazon, Zomato, etc.).
Your job is to determine the likelihood that a review was generated by AI (like ChatGPT) rather than written by a real human who used the product.

Analyze the provided review text for AI signals:
- AI signals: Overly perfect grammar, generic praise ("game-changer", "exceeded expectations"), lack of specific sensory details, overly formal structure, robotic enthusiasm.
- Human signals: Typos, specific product complaints/details, colloquial language, unique pacing.

CRITICAL INSTRUCTION: You MUST respond ONLY with a valid JSON object. Do not include any other text, markdown formatting, or code blocks outside the JSON.

Expected JSON schema:
{
  "score": <number between 0 and 100, representing % likelihood of being AI>,
  "explanation": "<A 2-sentence explanation of your conclusion>",
  "signals": [
    "<Signal 1 (e.g., 'Generic praise without specific details')>",
    "<Signal 2>"
  ]
}`;

  const userMessage = `Analyze this review:\n\n${sanitized}`;

  const makeRequest = async (model) => fetch("https://api.groq.com/openai/v1/chat/completions", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "Authorization": `Bearer ${groqKey.trim()}`
    },
    body: JSON.stringify({
      model,
      response_format: { type: "json_object" },
      messages: [
        { role: "system", content: systemMessage },
        { role: "user", content: userMessage }
      ],
      temperature: 0.2,
      max_tokens: 500,
    }),
  });

  try {
    let apiRes = await makeRequest("llama-3.3-70b-versatile");

    if (!apiRes.ok) {
      const errText = await apiRes.clone().text();
      let errJson;
      try { errJson = JSON.parse(errText); } catch { errJson = {}; }
      if (errJson?.error?.code === "model_not_found" || apiRes.status === 404) {
        apiRes = await makeRequest("llama-3.1-8b-instant");
      }
      if (!apiRes.ok) throw new Error(errJson?.error?.message || "Groq API error");
    }

    const json = await apiRes.json();
    let textOut = json.choices?.[0]?.message?.content || "";
    if (!textOut) throw new Error("Empty response from AI");
    
    // Attempt to parse JSON response
    let parsedResult;
    try {
      parsedResult = JSON.parse(textOut);
    } catch (e) {
      throw new Error("Failed to parse analysis results.");
    }

    res.status(200).json(parsedResult);
  } catch (error) {
    console.error("Fake Review Detector Error:", error);
    res.status(500).json({ error: error.message || "Something went wrong" });
  }
}
