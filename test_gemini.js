const fs = require('fs');
require('dotenv').config({path: '.env.example'}); // wait I need the actual key
// wait, I don't have the API key in .env.example. Let's just create a generic test if there's a real key
